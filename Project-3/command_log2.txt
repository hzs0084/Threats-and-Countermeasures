# Command Log v2

1. sudo tee /var/www/html/uploads/.htaccess << 'EOF'
AddType application/x-httpd-php .png .jpg .jpeg .txt .pdf
php_flag engine on
EOF

2. sudo tee /etc/apache2/conf-available/uploads.conf << 'EOF'
<Directory /var/www/html/uploads>
    AllowOverride All
    Options -Indexes
</Directory>
EOF

3. sudo a2enconf uploads

4. sudo systemctl restart apache2

5. http://192.168.56.21/uploads/shell.png?cmd=id

6. sudo tee /var/www/html/uploads/.htaccess << 'EOF'
AddType application/x-httpd-php .png .jpg .jpeg .txt .pdf
php_flag engine on
EOF

7. sudo tee /etc/apache2/conf-available/uploads.conf << 'EOF'
<Directory /var/www/html/uploads>
    AllowOverride All
    Options -Indexes
</Directory>
EOF

8. sudo a2enconf uploads

9. sudo systemctl restart apache2

10. http://192.168.56.21/uploads/shell.png?cmd=id

11. sudo apt install -y snort

12. sudo sed -i 's/ipvar HOME_NET any/ipvar HOME_NET 192.168.56.0\/24/' /etc/snort/snort.conf

13. sudo sed -i 's/DEBIAN_SNORT_HOME_NET="192.168.0.0\/16"/DEBIAN_SNORT_HOME_NET="192.168.56.0\/24"/' /etc/snort/snort.debian.conf

14. sudo sed -i 's/DEBIAN_SNORT_INTERFACE="enp0s3"/DEBIAN_SNORT_INTERFACE="enp0s8"/' /etc/snort/snort.debian.conf

15. sudo grep -E "HOME_NET|INTERFACE" /etc/snort/snort.debian.conf

16. sudo sed -i 's|# include \$RULE_PATH/local.rules|include $RULE_PATH/local.rules|' /etc/snort/snort.conf

17. sudo grep "local.rules" /etc/snort/snort.conf

18. sudo tee /etc/snort/rules/local.rules << 'EOF'
# ACME IT Corp - Snort Local Rules

# Detect nmap SYN scan - lowered threshold to 5 packets in 2 seconds
alert tcp any any -> $HOME_NET any (msg:"NMAP SYN Scan Detected"; flags:S; threshold:type threshold, track by_src, count 5, seconds 2; sid:1000001; rev:1;)

# Detect nmap NULL scan
alert tcp any any -> $HOME_NET any (msg:"NMAP NULL Scan Detected"; flags:0; sid:1000002; rev:1;)

# Detect nmap XMAS scan
alert tcp any any -> $HOME_NET any (msg:"NMAP XMAS Scan Detected"; flags:FPU; sid:1000003; rev:1;)

# Detect nmap FIN scan
alert tcp any any -> $HOME_NET any (msg:"NMAP FIN Scan Detected"; flags:F; sid:1000004; rev:1;)

# Detect ping sweep
alert icmp any any -> $HOME_NET any (msg:"ICMP Ping Sweep Detected"; threshold:type threshold, track by_src, count 5, seconds 2; sid:1000005; rev:1;)

# Detect FTP login attempts
alert tcp any any -> $HOME_NET 21 (msg:"FTP Login Attempt"; content:"USER"; nocase; sid:1000006; rev:1;)

# Detect vsftpd backdoor trigger
alert tcp any any -> $HOME_NET 21 (msg:"VSFTPD Backdoor Attempt - Smiley Trigger"; content:":)"; sid:1000007; rev:1;)

# Detect vsftpd backdoor shell connection
alert tcp any any -> $HOME_NET 6200 (msg:"VSFTPD Backdoor Shell Connection on Port 6200"; sid:1000008; rev:1;)

# Detect gobuster
alert tcp any any -> $HOME_NET 80 (msg:"Directory Enumeration Tool Detected - gobuster"; content:"gobuster"; nocase; http_header; sid:1000009; rev:1;)

# Detect dirbuster
alert tcp any any -> $HOME_NET 80 (msg:"Directory Enumeration Tool Detected - DirBuster"; content:"DirBuster"; nocase; http_header; sid:1000010; rev:1;)

# Detect PHP webshell
alert tcp any any -> $HOME_NET 80 (msg:"PHP Webshell cmd Parameter Detected"; content:"cmd="; nocase; sid:1000011; rev:1;)

# Detect outbound reverse shell
alert tcp $HOME_NET any -> any any (msg:"Possible Reverse Shell Outbound"; content:"/bin/sh"; sid:1000012; rev:1;)

# Detect SSH brute force
alert tcp any any -> $HOME_NET 22 (msg:"SSH Brute Force Attempt"; threshold:type threshold, track by_src, count 5, seconds 60; sid:1000013; rev:1;)

# Detect nikto
alert tcp any any -> $HOME_NET 80 (msg:"Nikto Scanner Detected"; content:"Nikto"; nocase; http_header; sid:1000014; rev:1;)
EOF

19. sudo systemctl restart snort

20. sudo tail -f /var/log/snort/snort.alert.fast

21. sudo useradd -r -s /usr/sbin/nologin snort 2>/dev/null || true

22. sudo mkdir -p /var/log/snort

23. sudo chown -R snort:snort /var/log/snort

24. sudo chmod 750 /var/log/snort

25. sudo tee /etc/systemd/system/snort.service << 'EOF'
[Unit]
Description=Snort NIDS
After=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/snort -q -u snort -g snort -c /etc/snort/snort.conf -i enp0s8 -l /var/log/snort
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

26. sudo systemctl daemon-reload

27. sudo systemctl enable snort

28. sudo systemctl start snort

29. sudo systemctl status snort | grep "snort -"

30. # Should show: /usr/sbin/snort -q -u snort -g snort -c /etc/snort/snort.conf -i enp0s8 -l /var/log/snort

31. sudo snort -T -i enp0s8 -c /etc/snort/snort.conf 2>&1 | tail -5

32. # Should end with: Snort successfully validated the configuration!

33. sudo tail -f /var/log/snort/snort.alert.fast

34. sudo nmap -sS 192.168.56.21

35. ssh wronguser@192.168.56.21

36. ftp 192.168.56.21

37. ftp 192.168.56.21

38. nc 192.168.56.21 6200

39. /etc/snort/snort.conf          # Main config

40. /etc/snort/snort.debian.conf   # Debian sysv override (interface + HOME_NET)

41. /etc/snort/rules/local.rules   # Our custom ACME rules

42. /var/log/snort/snort.alert.fast # Alert log (watch this)

43. /etc/systemd/system/snort.service # Systemd service definition

44. sudo ufw enable

45. sudo ufw default deny incoming

46. sudo ufw default allow outgoing

47. sudo ufw allow 22/tcp

48. sudo ufw allow 80/tcp

49. sudo ufw allow 3000/tcp

50. sudo ufw allow from 172.16.0.0/24

51. sudo ufw status verbose

52. sudo tee /etc/iptables-slowdown.sh << 'EOF'
#!/bin/bash

# Rate limit new TCP connections - makes nmap timing unreliable
iptables -I INPUT -p tcp --syn -m limit --limit 10/second --limit-burst 20 -j ACCEPT
iptables -I INPUT -p tcp --syn -j DROP

# Rate limit ICMP - slows ping sweeps
iptables -I INPUT -p icmp --icmp-type echo-request -m limit --limit 1/second --limit-burst 5 -j ACCEPT
iptables -I INPUT -p icmp --icmp-type echo-request -j DROP

# This should be noticeably slower than before
sudo nmap -sS 192.168.56.21
# Drop NULL scan packets silently
iptables -A INPUT -p tcp --tcp-flags ALL NONE -j DROP

# Drop XMAS scan packets silently
iptables -A INPUT -p tcp --tcp-flags ALL ALL -j DROP

echo "Firewall slowdown rules applied"
EOF

53. sudo chmod +x /etc/iptables-slowdown.sh

54. sudo bash /etc/iptables-slowdown.sh

55. sudo apt install -y iptables-persistent

56. sudo netfilter-persistent save

57. sudo ufw status verbose

58. sudo iptables -L INPUT -n --line-numbers | head -20
